import { StyleSheet } from "react-native";

export const TextStyles = StyleSheet.create({
  text: {
    color: "white",
    fontSize: 20,
    alignSelf: "center",
    textAlign: "center",
    letterSpacing: -0.75,
  },
});
